{
    "name": "root",
    "version": "0.0.0",
    "description": "Multi-package web application template",
    "private": true,
    "scripts": {
        "bootstrap": "npx lerna bootstrap --hoist",
        "prestart": "npm run bootstrap",
        "start": "startup start",
        "prebuild": "npm run bootstrap",
        "build": "startup build",
        "lint": "startup lint",
        "test": "startup test"
    },
    "devDependencies": {
        "@servicetitan/startup": "~10.4.2"
    }
}
