{
    "name": "root",
    "version": "0.0.0",
    "description": "Multi-package web application template",
    "private": true,
    "scripts": {
        "bootstrap": "npx lerna bootstrap --hoist",
        "prestart": "npm run bootstrap",
        "start": "startup start",
        "prebuild": "npm run bootstrap",
        "build": "startup build",
        "serve": "npm run build && lerna run serve",
        "lint": "startup lint",
        "test": "startup test"
    },
    "devDependencies": {
        "@servicetitan/startup": "~12.6.0"
    }
}
